server:
  port: 9090


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/july
    username: root
    password: cos1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace